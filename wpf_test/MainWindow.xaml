<Window x:Class="wpf_test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_test"
        mc:Ignorable="d"
        DataContext="{Binding MainWindowViewModel, Source={StaticResource ServiceLocator}}"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="{Binding Text}" FontSize="20" Foreground="Red"/>
        <Button Content="button" Command="{Binding ButtonCommand}" Width="100" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>

    </StackPanel>
</Window>
